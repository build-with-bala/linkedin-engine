{
  "name": "@linkedin-engine/cli",
  "version": "0.1.0",
  "description": "Command-line interface for the LinkedIn engine.",
  "type": "module",
  "bin": { "linkedin-engine": "./dist/index.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@linkedin-engine/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.17.12",
    "typescript": "^5.7.3"
  }
}
